const mutations = {
  setUsers (state, users) {
    state.users = users
  },
  setUserToken (state, token) {
    state.userToken = token
  },
  setUser (state, user) {
    state.user = user
  }
}

export default mutations
