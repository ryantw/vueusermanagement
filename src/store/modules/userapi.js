const state = {
    userToken: 'TMPTMP'
};

const actions = {

}

const mutations = {

};

const getters = {
    getUserToken: state => {
        return state.userToken;
    }
};

export default {
    state,
    actions,
    mutations,
    getters
}